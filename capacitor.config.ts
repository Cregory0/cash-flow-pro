import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cash.flow.pro.app',
  appName: 'Cash Flow Pro',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
